'use strict';

/**
 *  reference-slider controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::reference-slider.reference-slider');
